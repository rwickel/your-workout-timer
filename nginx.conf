server {
    listen 8090;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback so /wod?d=... works
    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
